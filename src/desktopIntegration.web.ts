import * as vscode from "vscode";

export function registerDesktopIntegrations(
  _context: vscode.ExtensionContext
): void {
  // Local processes and Codex CLI configuration are desktop-only capabilities.
}
